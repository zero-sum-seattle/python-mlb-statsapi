# python-mlb-statsapi 1.0.1

## Fixed

- Schedule queries using `gamePks` no longer require a date.
- Fixed both `Mlb.get_schedule()` and
  `Mlb.get_scheduled_games_by_date()` returning early before
  contacting the MLB Stats API.

Fixes #245.
